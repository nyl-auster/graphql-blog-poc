const PostService = require('./services/PostService')

const post = {
  title:"dernier test O",
  slug: "hello-world-12",
  content: "Je suis le contenu de l'article hello world",
  tagsIds: ["4567890XXX0997889", "67890xxx89087"]
}

// PostService.create(post).then(result => console.log(result))
// PostService.getAll().then(result => console.log(result))
